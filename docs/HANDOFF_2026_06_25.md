# Handoff — 25.6.2026

## ענף פעיל: `feature/ga4`

---

## מה הושלם היום

### 1. GTM + GA4 — הטמעה בקוד (`feat: add GTM + GA4 marketing events`)
- `index.html` — snippet של GTM-TP48Q5PK (head + noscript body)
- `src/lib/gtm.ts` — קובץ helper חדש, 5 אירועי שיווק, נפרד לגמרי מהמעקב הפנימי (Supabase)

| אירוע | קובץ |
|---|---|
| `sign_up` (email) | `src/contexts/AuthContext.tsx` |
| `sign_up` (Google OAuth) | `src/pages/AuthCallback.tsx` |
| `onboarding_complete` | `src/components/onboarding/OnboardingComplete.tsx` |
| `site_published` | `src/pages/PublishPaymentOld.tsx` ← הדף הפעיל בפועל |
| `connect_payment_click` | `src/components/payments/PayplusConnectForm.tsx` |
| `purchase` | `src/pages/StoreFront.tsx` |

### 2. תיקונים לפי סקירת מוטי (`fix: complete GTM sign_up and site_published coverage`)
- `sign_up` נתפס עכשיו גם ב-Google OAuth (לפי `user.created_at < 2 דקות`)
- `site_published` הועבר ל-`PublishPaymentOld.tsx` — הקובץ שבאמת טוען בנתיב `/publish-payment`

### 3. מיזוג שלב תבנית + צבע (`feat: merge template + color selection into single onboarding step`)
- `StepBrandStyle` (שלב 1 הישן) **הוסר לגמרי** מהתהליך
- `StepTemplate` עודכן — עכשיו כולל:
  - **6 תצוגות תבניות** שמתעדכנות בזמן אמת לפי הצבע הנבחר
  - **15 swatches** מוכנים
  - **color picker** + **שדה HEX ידני**
  - **העלאת קובץ** (תמונה / PDF / לוגו / חומר פרסומי) — סורקים ומחלצים צבע ראשי אוטומטית

---

## מה עוד צריך לעשות (לא נגענו)

### GTM — הגדרה בממשק (חובה לפני שיווק)
הקוד **שולח** אירועים ל-dataLayer אבל הם לא מגיעים ל-GA4 בלי הגדרה בממשק GTM:
1. כנס ל-tagmanager.google.com → קונטיינר GTM-TP48Q5PK
2. צור תג GA4 Configuration עם measurement ID: `G-NTE60FKTBN`
3. צור טריגר + תג לכל אירוע: `sign_up`, `onboarding_complete`, `site_published`, `connect_payment_click`, `purchase`
4. הגדר אותם כ-Conversions ב-GA4
5. פרסם את הקונטיינר

### Meta Pixel
- Business Manager של Siango מוגבל — נשלחה בקשת בדיקה ל-Meta
- ממתין לשחרור (1-3 ימי עסקים)
- כשיגיע Pixel ID — מוסיפים שורה אחת ב-`src/lib/gtm.ts`

### מיזוג לmain
- `feature/ga4` מוכן למיזוג — build נקי, ללא קונפליקטים
- סה"כ: 9 קבצים שונו, ~60 שורות

---

## מזהים חשובים
| שירות | מזהה |
|---|---|
| GTM | GTM-TP48Q5PK |
| GA4 | G-NTE60FKTBN |
| Meta Pixel | ממתין |
| Supabase (Siango) | ytqgeoviokgxxwalieev |
